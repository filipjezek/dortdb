/**
 * Built-in aggregate functions provided by the core engine - numeric
 * aggregates such as `sum`, `avg`, `min`, `max`, and `count`, plus the
 * collection aggregate `collect`.
 *
 * @packageDocumentation
 */

export * from './math.js';
export * from './data-structures.js';
