{
  "name": "@dortdb/core",
  "type": "module",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rollup -c rollup.config.js && tsc -d --emitDeclarationOnly"
  },
  "author": "dortdb",
  "license": "ISC",
  "devDependencies": {
    "rollup": "^4.16.4",
    "rollup-plugin-esbuild": "^6.1.1"
  },
  "dependencies": {
    "mnemonist": "^0.39.8"
  }
}
