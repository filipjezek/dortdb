import { XQueryIdentifier } from '../ast/expression.js';

export const DOT = XQueryIdentifier.fromParts(['fs', 'dot']);
export const POS = XQueryIdentifier.fromParts(['fs', 'position']);
export const LEN = XQueryIdentifier.fromParts(['fs', 'last']);
